# Project Agent Instructions

- Commit completed feature-sized changes iteratively. When a coherent feature or cleanup is validated, create a Git commit before moving on to the next feature.
- Keep the root `data/` folder out of Git; it is local dataset material.
- Run `uv run ruff format`, `uv run ruff check --fix`, and `uv run pytest` before declaring code work complete.
